# GPT Image Playground 客户使用说明

## 一、准备工作

1. 安装 Node.js 20 或更高版本。
   下载地址：https://nodejs.org/

2. 解压项目压缩包。

3. 双击 `start-windows.bat`。

首次运行会自动安装依赖，可能需要几分钟。后续再次双击会直接启动。

## 发包人员说明

发给客户前，双击：

```text
package-customer.bat
```

脚本会自动生成：

```text
dist/gpt-image-playground-customer.zip
```

把这个 zip 发给客户即可。

打包脚本会自动排除这些目录或文件：

```text
.git
.next
node_modules
generated-images
dev-server.log
dev-server.err.log
```

客户首次双击 `start-windows.bat` 时会自动安装依赖，所以不需要把 `node_modules` 一起打包。

## 二、打开网页

启动后浏览器会自动打开：

```text
http://localhost:3000
```

如果没有自动打开，请手动复制上面的地址到浏览器。

运行期间不要关闭黑色命令窗口。关闭窗口后，网页服务也会停止。

## 三、填写 API 设置

进入网页后，点击右上角 `API 设置`。

填写：

```text
API Key：你的 API Key
API URL：你的 OpenAI 兼容接口地址
```

如果使用 OpenAI 官方接口，API URL 可以填写：

```text
https://api.openai.com/v1
```

如果使用第三方兼容接口，API URL 通常也需要以 `/v1` 结尾，例如：

```text
https://你的域名/v1
```

不要填写管理后台首页地址，否则会返回 HTML 页面，无法生成图片。

## 四、常见问题

### 1. 提示未检测到 Node.js

请安装 Node.js 20 或更高版本，然后重新双击 `start-windows.bat`。

### 2. 依赖安装失败

通常是网络问题。请确认电脑可以访问 npm，然后重新双击 `start-windows.bat`。

### 3. API 返回 HTML 页面

说明 `API URL` 填错了。请填写接口根地址，通常以 `/v1` 结尾。

### 4. 端口被占用

如果 `http://localhost:3000` 打不开，可能是 3000 端口被其他程序占用。
请关闭其他正在运行的本项目窗口后重试。

## 五、关闭软件

关闭启动时打开的黑色命令窗口即可。
